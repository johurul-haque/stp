import { Atma, Inter, Manrope } from 'next/font/google';

export const atma = Atma({
  subsets: ['latin'],
  weight: ['300', '400', '600', '700'],
});
export const manrope = Manrope({ subsets: ['latin'] });
export const inter = Inter({ subsets: ['latin'] });
